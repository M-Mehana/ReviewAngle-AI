# ReviewAngle AI implementation

## Plan and status

- [x] Arabic-first Phase 2: four genuine MSA angles, five preserved Egyptian angles, portfolio/evidence/semantic gates, regression, real Supabase and secret audit passed.
- [ ] Phase 3: not started.

- [x] Inspect repository: empty GitHub repository, cloned locally.
- [x] Define architecture and implementation decisions.
- [x] M1: Next.js foundation, ingestion, normalization, fixtures, deterministic tests.
- [x] M2: Structured Responses pipeline, aggregation, scores, traceability, mock integration tests.
- [x] M3: Supabase schema/RLS, email authentication, persistence, quotas, durable analysis steps.
- [x] M4: Responsive bilingual workspace, import preview, results, evidence, follow-ups, exports.
- [x] M5: Safe public HTML connectors and manual fallback.
- [x] M6: Run tests/build, exercise desktop/mobile/Arabic workflows, deployment documentation.

## Decisions

- Local demo mode is explicit, synthetic, isolated from authenticated production data, and uses a deterministic fixture provider; it never impersonates live AI.
- Production requires Supabase and OpenAI credentials. No secrets are committed.
- Process bounded batches via resumable HTTP steps, persisting progress after every step. This avoids relying on background work surviving a serverless request.
- CSV and TXT are first-class; XLSX is optional and deferred unless it adds little risk.
- Counts and scores are application-computed. Model output may select evidence only from validated input IDs.
- URL import only reads public HTML, observes robots policy, and does not execute third-party JavaScript.
- Next.js standalone Docker deployment is the primary portable deployment target; no deployment is performed in this implementation task.

## Validation log

- Production build and TypeScript validation passed on Node 24, Next.js 16.3.4, React 19.2.8.
- 50 unit/integration tests pass with no paid API calls. Includes real Responses SDK schema helper, mocked model responses, bounded extraction, quote validation, CSV, normalization, duplicates, phrase counts, score caps, connector HTML, DNS pinning, SSRF redirects/timeouts/size caps, and embedded PostgreSQL migration/RLS/quotas/leases/relational evidence.
- Four browser workflows passed: desktop analysis, evidence, translation setup recovery, hooks, UGC follow-ups, saving, JSON export and persisted history; Arabic mobile layout at 390px; malformed CSV and missing column recovery; direct paste and CSV analysis with actual clipboard verification.
- Fixed localhost origin mismatch exposed by the browser test, in-dialog error visibility, and scroll reset between sections.
- Original fixture examples are labeled synthetic throughout. Demo analysis is deterministic and never presented as live OpenAI output.
- Standalone production server smoke test passed: root returned 200; demo stayed disabled even with DEMO_MODE=true; the unconfigured production API returned the intended 503 instead of granting demo access.

## Repository delivery

- The baseline was subsequently published to GitHub `main` as `a206cf116608fe1aeda4b1331d575a5c53c14700`. The initial upload permission failure was resolved before Phase 2.
- Phase 2 continues that baseline in the existing configured checkout; it does not rebuild the project. See the Phase 2 validation record below.

## External validation still required before launch

- Real Supabase email delivery/confirmation was user-confirmed; automated HTTP/session/persistence validation passed locally. See the current Phase 2 record below.
- Build/run the Docker image on a Docker host. Docker was not installed here; Next.js standalone production output was built successfully.
- Permit and live-validate each merchant domain before enabling URL imports. Widget HTML fixtures pass, but no live merchant site is certified.
- XLSX, Google login and PDF export remain intentionally outside this MVP.

## Phase 2 staging validation (2026-09-06)

- Continued the existing `main` implementation from `a206cf116608fe1aeda4b1331d575a5c53c14700` in the configured checkout. Reused its ignored OpenAI key without changing it.
- Added strict opt-in local staging: real OpenAI provider; arbitrary Paste/CSV/TXT; ignored `.local/staging/` persistence; separate synthetic demo storage/provider; localhost host/origin/proxy checks; loopback-only launcher; production refusal in both configuration and request handling. Supabase authentication/persistence/quotas are bypassed only in local staging.
- Unit/integration suite: 69 passed, including 19 local staging tests. Typecheck and final production build passed with filesystem caching disabled.
- Browser suite: all 4 synthetic regression workflows passed; all 3 live staging workflows passed. Fixed the desktop test to wait for completed results rather than the heading rendered during processing.
- Real `gpt-6-astra` runs: English (3 reviews, 9 themes, 5 angles) and Egyptian Arabic (3 reviews, 10 themes, 5 angles). Verified exact source quotes, review/theme IDs, counts, insight/angle evidence unions, language output, browser evidence dialogs and persisted history. TXT import/persistence and hostile request rejection passed as well.
- Negative production checks: both the staging launcher and `next start` refused `LOCAL_STAGING_MODE=true` under `NODE_ENV=production`.
- Supabase project `hdtkzoksuxdmabxfxeyu`: confirmed applied migration `20260906194349_initial_schema` and RLS enabled on all 11 public tables. No schema changes or migration replay were performed. Supabase email/session and remote persistence remain separate launch checks because the local checkout has no service-role or public Supabase configuration.
- Secret audit detected environment values in three existing Turbopack disk-cache files, not in source, diffs, browser logs, local analysis data or deployment output. Disabled filesystem caching for development and builds, and excluded env/local data from deployment tracing. Cleanup was blocked by automatic approval review; manual cache removal and a clean audit are required before declaring the artifact audit complete. The audit prints filenames/counts only.

## Earlier Phase 2 checkpoint (2026-09-07; superseded by current validation below)

- Configured the existing staging project's public URL and legacy anon key in ignored `.env.local`; the private OpenAI value was preserved. The server service-role key is still absent. Auth settings confirm email signup is enabled and requires confirmation; anonymous Data API access is rejected.
- Executed `scripts/validate-supabase-database.sql` against `hdtkzoksuxdmabxfxeyu`: real service-role SQL writes, repeated snapshot writes, all 11 table ownership policies, profile creation, relational evidence/followups, quota/idempotency/request limits, and lease claim/release passed. Temporary test users/data were rolled back. No migration/schema was changed. These are hosted database-role checks, not HTTP Auth/session verification.
- Added `pnpm test:supabase`: an opt-in real HTTP/browser validation of password login for two temporary confirmed accounts, refresh, authenticated OpenAI analysis, relational persistence, RLS, quota/leases and browser history. Tokens stay in memory, raw transport diagnostics are suppressed, and traces/screenshots/video are disabled. Running it currently reports the missing `SUPABASE_SERVICE_ROLE_KEY`; it is not counted as passed. Public signup/email delivery remains a separate check requiring a user-provided test mailbox. The product's email-OTP UI is unchanged.
- Added 50/200-review deterministic load tests: 16/56 bounded provider-double calls, 94/575 ms in the recorded run, no paid calls. All rows survive normalization/duplicate checks, evidence quotes/IDs/counts remain valid, and scores are application-computed. Fixtures are explicitly NON-CUSTOMER performance data. A blank genuine-review CSV template and 200-row load fixture can be generated with `node scripts/prepare-validation-data.mjs`.
- Regression: 78 tests and typecheck pass; production build and all 4 default browser workflows pass. New production-context tests verify missing/invalid sessions fail closed and do not activate a local/demo bypass. Prior live English/Egyptian Arabic OpenAI results remain valid; provider prompts and product behavior were not changed.
- Secret audit now distinguishes the intentionally public Supabase anon/publishable browser key from private keys, including a regression for service-role values mistakenly placed in the public slot. The original three ignored Turbopack cache files still match the private key. Automatic approval review again blocked their deletion; manual cleanup is pending. No private-key matches were found in source, diffs, deployment output or validation/test artifacts outside those cache files.
- Genuine 50–200-review validation is pending user-provided CSV/TXT. No restricted scraping, invented customer reviews, or prompt tuning to synthetic data was performed.
- At this checkpoint, Phase 2 was not done: HTTP Auth/session/persistence, public email confirmation, cache cleanup and genuine-review quality were outstanding. The current record below supersedes these historical blockers.

## Full-dataset checkpoint before nano resume (2026-09-07)

- The configured checkout now has the required credentials in ignored `.env.local`. The existing OpenAI key was reused without rotation or replacement.
- The local real Supabase HTTP/browser report records successful password login, session refresh, authenticated OpenAI analysis, nine relational persistence tables, owner isolation, quotas/leases and browser history. Public signup/email delivery and confirmation were separately verified by the user, not by the admin-created-account test.
- The user removed the old Turbopack caches; the initial rerun audited 677 files with zero private-secret matches. Filesystem caches remain disabled.
- Genuine-review validation used two separate private local CSVs and real OpenAI through the existing local staging pipeline. Customer data and reports remain under ignored `.local/`; genuine reviews were not sent to Supabase or committed.
- Found two Arabic near-duplicate pairs missed because of letter elongation, spelling and joined-word differences. Added comparison-only folding and bounded edit similarity for long Arabic copies, with negation protection; originals and exact-quote evidence remain unchanged. New synthetic regressions reproduce the miss and protect short praise, distinct experiences and changed negation. All 97 Arabic rows normalize; two near duplicates leave 95 for analysis. Four exact English duplicates leave 96 of 100 rows; the fix does not change that dataset's selection.
- Two English intelligence responses referenced missing evidence and were rejected before persistence. Constrained insight/angle structured-output schemas to the supplied theme IDs and added request-schema/unknown-ID regression coverage. No prose prompt tuning was performed. Reused validated full-dataset extraction/grouping when rerunning these downstream stages.
- English → English: final constrained run passed on 96 reviews, 242 themes and ten distinct angles. Exact quotes, review/theme IDs, unique counts, evidence unions and deterministic scores passed. Manual review found natural English, qualified persona hypotheses and no material unsupported claims or invented statistics. Single-review themes remain visible as limited observations; they do not independently drive the angles.
- English → Egyptian Arabic: the first full run passed on 96 reviews, 245 themes and ten distinct angles, with clean evidence checks and natural Egyptian wording. The constrained-schema rerun completed intelligence but is blocked at angle generation by exhausted API credits; it is not counted as final completion.
- Arabic → Arabic: the first run rejected an invalid extraction ID. A fresh run after duplicate correction extracted all 95 distinct reviews and completed theme grouping for 80 of them before credits were exhausted. Final Arabic intelligence/angles and their semantic quality remain unverified.
- Final code regression: 82 unit/integration tests, typecheck, production build and all four default browser workflows passed. Two earlier real Supabase HTTP/browser reruns passed; the latest run after the schema change failed during authenticated OpenAI analysis when API credits were exhausted. Public email confirmation remains separately user-confirmed.
- A no-retry diagnostic confirmed HTTP 429 with `credit_balance_exhausted` and `insufficient_quota`; this is not transient throttling. No key was rotated or replaced. Paid requests stopped, and checkpoints are resumable.
- The private-secret scanner reports zero matches across source, diffs, local data, logs and build/test artifacts. Customer inputs, generated reports and environment files remain ignored and untracked. The final delivery reports post-push CI separately; CI does not run paid genuine-review or hosted Supabase checks.
- At this checkpoint Phase 2 was not done: credits, the remaining Arabic/Egyptian runs and a final Supabase rerun were outstanding. The nano continuation below supersedes those blockers. No Phase 3 work was started.

## Previous Phase 2 result after nano resume (2026-09-07)

- Credits were restored. Reused the existing ignored key with configured `OPENAI_MODEL=gpt-5-nano`; no rotation, replacement, exposure or more expensive model switch. Returned model identifier: `gpt-5-nano-2025-08-07`.
- Preserved English's passing 96-review/ten-angle `gpt-6-astra` validation without rerunning it. Preserved Egyptian's 96-review extraction/grouping and completed insights, and Arabic's 95 extractions plus grouping checkpoint at 80 reviews. New requests used nano; these are mixed-model saved-state continuations.
- Egyptian final generation completed with ten angles. Mechanical integrity passed for 361 theme quote links, 245 themes, review/theme IDs, unique counts, insight/angle unions and deterministic Evidence Scores. Manual quality **failed**: guaranteed fit from individual fit reports; generic quality praise expanded into durability; one quick-delivery report expanded into a delivery-time/reliability promise; cooling and ventilation claims exceeding cited support. No explicit invented percentages or numeric customer totals were found, but unsupported prevalence wording also appeared.
- Egyptian localization **failed**: several incoherent CTAs/scenes, grammatical mismatches, and MSA phrasing mixed with dialect particles. No identical hooks were found, but creative execution was repetitive and all alternative-hook arrays were empty. Shipping stayed separately classified; the shipping promise itself was unsupported. Nano quality was materially worse than the earlier passing Egyptian run. The gate was not relaxed.
- Arabic grouping **failed to advance**: a nano response used 5,241 input tokens and 11,968 output tokens, all output recorded as reasoning, with no parsed structured result. The app rejected it; no invalid grouping was saved. Checkpoint remains 80/95, with all 95 extractions intact. Final Arabic intelligence, angles, localization and hallucination checks are incomplete and cannot be called passed.
- Real `pnpm test:supabase` **passed with nano**: password login, renewal, authenticated OpenAI analysis, relational persistence, owner RLS isolation, quotas/leases and browser history. Public email delivery/signup confirmation was separately user-confirmed; this automated admin-account test does not replace that manual check.
- New genuine-resume work used two recorded SDK operations, with 21,009 input and 20,649 output tokens returned (41,658 total; 19,136 reasoning). The successful Supabase workflow adds approximately five small OpenAI operations; its token usage was not captured. SDK retries can add wire attempts, and no dollar cost is available from response metadata.
- No product source or prose prompt changes in this continuation. Previous source validation remains 82 passing tests, typecheck, build and four browser workflows; CI for `aa84108bb759c7ecb7e029cffa335b2826bde30a` passed. Local validation harnesses, detailed quality findings and source data remain ignored. Private-secret artifact audit remains clean.
- **Phase 2: NOT DONE.** Current blockers are unacceptable nano Egyptian claims/localization and incomplete nano Arabic grouping/output. Credits and Supabase are no longer blockers. No automatic upgrade or Phase 3 work was performed. Documentation changes remain local because the requested condition for a passing closeout commit was not met.

## Previous Phase 2 result with per-stage routing (2026-09-07)

- Added minimal optional `OPENAI_MODEL_FAST` and `OPENAI_MODEL_QUALITY` overrides. Extraction/grouping select FAST; intelligence, angles, hooks, UGC and translation select QUALITY. Each blank/unset override uses legacy `OPENAI_MODEL`, then the unchanged default. New project metadata lists configured models. No prose prompts, structured schemas, evidence restrictions or quality gates changed. Nine regression tests cover all stages and fallback behavior.
- User approved `gpt-5.6-luna` FAST and clarified `gpt-5.6-terra` QUALITY for configuration and validation. Only these model settings were added to ignored `.env.local`; the API key was preserved. The example file contains blank overrides and example comments. Legacy nano remains a fallback only and was not used for new work.
- Preserved the passing English 96-review/ten-angle Astra result without new requests. Reused Egyptian's 96 Astra extractions, grouping and 18 valid insights; Terra generated ten new angles in one request. Reused all 95 Arabic Astra extractions and grouping through 80; Luna finished the remaining 15 reviews in two requests, then Terra completed twelve insights and ten angles in two requests. Actual returned model identifiers matched the requested tier identifiers. Private reports record stage provenance; these are mixed-model continuations.
- Mechanical evidence integrity passed for both completed runs: Egyptian 245 themes/361 quote links; Arabic 317 themes/416 quote links. Review/theme IDs, exact source substrings, unique counts, evidence unions and application-computed scores passed. Single-review themes remain flagged and scores were not inflated.
- Egyptian semantic quality **did not pass**. The gift hook promises daily adoption without evidence. General comfort, after-work comfort and sofa-to-bed concepts overlap. Main hooks/copy sound substantially more natural in Egyptian than nano, but visible angle types remain English and several insight summaries remain MSA. No unsupported universal-fit, durability, temperature reduction, ventilation or delivery guarantee was found in this rerun; subjective cooling wording is supported.
- Arabic structured output and natural standard Arabic **passed**, but creative quality **did not pass**. Two angles repeat the empty-bottle/new-bottle repurchase concept and similar hooks. Another proposes day-1/day-7/day-10 eye imagery and a comparison without verified visual evidence; disclaiming creator use does not authenticate a results timeline. Textual results are generally attributed to reviews with variability acknowledged. No invented percentages/customer totals or seller/shipping leakage was found in either rerun. The identified unsupported claims/visuals prevent a clean overall hallucination verdict.
- Regression: **91 tests, typecheck, production build and all four browser workflows passed**. Fresh real Supabase auth/login, session refresh, authenticated analysis, nine-table persistence, RLS, quotas/leases and history **passed** using the routed model configuration. Public email signup/confirmation remains separately user-confirmed. Secret-cache cleanup remains resolved; the artifact audit reported **737 files, zero private-secret matches** before final documentation/report updates.
- New genuine continuation used five recorded SDK operations: **57,989 input + 12,807 output = 70,796 tokens**. Supabase adds approximately five small model operations without captured token totals. SDK retries may add wire attempts; actual dollar cost is unavailable.
- **Phase 2: NOT DONE.** Remaining blockers are the specific Egyptian claim/localization/repetition failures and Arabic visual-evidence/repetition failures above. No further nano work, paid evidence reprocessing or Phase 3 work occurred. Code/tests/docs are saved locally; no new commit/push was made because delivery was conditioned on every quality gate passing. Existing `aa84108bb759c7ecb7e029cffa335b2826bde30a` CI was rechecked and remains successful; this does not validate uncommitted routing changes.

## Earlier Phase 2 guardrail attempt (2026-09-07; superseded by the Arabic-first closeout)

- Preserved the uncommitted routing implementation and all genuine evidence/checkpoints. A previously authorized Sol intelligence/angles attempt completed before the user changed the objective; its results remain private and were not counted as semantic passes. The subsequent guardrail attempt used the user's current FAST=Luna, QUALITY=Luna, optional FALLBACK=Terra settings. The API key was only read, never changed or exposed.
- Added deterministic claim-family checks for timelines, usage frequency, absolutes, fit, durability, temperature concepts, logistics, health and visual results; added product scope, whole-angle localization and normalized concept/text/evidence distinctness checks. Evidence text is never normalized in storage. Prompts now explicitly address the demonstrated claim expansions, repeated motivations and incomplete localization.
- Initial accepted items are retained before repairs. Only rejected items receive one QUALITY repair and at most one configured FALLBACK repair, with their own evidence and accepted-concept summaries. Final failures are omitted. Blank FALLBACK remains disabled. Intelligence and followups are checked too. Structured ID restrictions and deterministic scores remain unchanged. Safe per-stage model/token and selection telemetry is stored without private content.
- 124 unit/integration tests, typecheck, build and four browser workflows passed. Fresh real Supabase login/session, authenticated analysis, persistence, RLS, quota/lease and history passed under final Luna configuration with optional selective Terra fallback. Existing public email confirmation remains user-confirmed; no migration or schema change was made.
- No genuine English call, extraction or grouping was repeated. Evidence hashes verify unchanged Egyptian/Arabic source reviews, extractions and themes during completed stages. Egyptian completed six insights and five angles; mechanical review IDs/theme IDs/exact source quotes/counts/unions/scores passed. Arabic saved five insights; its remaining angle repair was stopped when the Egyptian semantic failure was discovered. Five already-accepted Arabic candidates were recovered offline from recorded responses, without generation, in the ignored recovery checkpoint.
- **Material miss, Egyptian:** the retained cooling-feel angle's UGC instructs a person to describe their personal experience. It received a Luna repair and then a Terra fallback that passed deterministic checks, but no actual creator experience was supplied. The guards do not establish testimonial provenance. A first-pass sleepwear angle also uses personal movement commentary in a staged concept.
- **Material miss, Arabic:** an initially accepted non-oily-serum angle adds eyelash use although both non-oily supporting reviews describe nail serum. It received no repair/fallback because product scope passed; product entity matching is missing. Three recovered Terra repairs also converge on product shots with written review cards; distinctness checks missed repetitive execution. The partial set is not a final Arabic pass.
- **False positives:** the visual-results regex rejects ordinary Egyptian wording meaning that fabric feel matters. Some disclaimer/negative and pain-description wording also triggers broad absolute/timeline rules. More expensive regeneration cannot reliably resolve a validator that misclassifies safe prose. No rule was weakened or prompt changed after the material failure.
- Recorded genuine attempt totals: 29 initial candidates; 11 first-pass acceptances; 16 completed QUALITY repairs; 12 Terra fallback requests started, 11 completed and one interrupted; five confirmed omissions; three unfinished candidates. Completed response usage: 108,330 input + 23,541 output = 131,871 tokens, including 8,926 reasoning tokens. The interrupted request may be billable; existing Supabase report does not export its additional model/token/fallback totals. Counts include intelligence and angles, not just angles.
- Secret audit passed with zero matches, and customer-text comparison against the working changes passed. Environment files, genuine CSVs and all private checkpoints remain ignored/untracked. Existing main remains `aa84108bb759c7ecb7e029cffa335b2826bde30a`; no new commit/push or CI was initiated because the required quality gate failed.
- **Phase 2 NOT DONE.** Stop reached as requested. Remaining blockers: testimonial provenance, product identity, repeated creative execution, false-positive guards, and completion/review of the interrupted Arabic candidates. No further paid genuine attempt, model escalation or Phase 3 work was performed.

## Arabic-first Phase 2 closeout (2026-09-09)

**Phase 2 DONE locally.** Generated output supports only Arabic MSA (`ar`), Egyptian Arabic (`ar-EG`) and Gulf/Saudi Arabic (`ar-SA`). Neutral Arabic is the default; Arabic result dialogs remain RTL even with the English interface. English/Arabic/mixed source reviews and original-language evidence remain supported. English output is disabled for new analyses; historical English projects remain readable/exportable without paid regeneration. The earlier 96-review English output pass is historical evidence, not a current completion gate.

The genuine MSA set contains four distinct supported motivations and four executions: purchase confidence/social proof, brow density/routine, lash length/close-up and non-oily texture/product feature. Exactly one execution explicitly presents customer reviews. Two existing angles were preserved; only two excess social-proof slots were repaired. Egyptian's five distinct angles and all evidence stages were preserved without regeneration. Mild formality in existing Egyptian summaries is nonblocking for this scope. Gulf/Saudi support passed deterministic checks; no large paid Gulf validation was required.

FAST and QUALITY remain `gpt-5.6-luna`; optional FALLBACK remains `gpt-5.6-terra`. This genuine attempt made two Luna repairs and two Terra fallbacks (8,110 reported tokens), with no extraction/grouping/intelligence/English/Egyptian regeneration. Both accepted replacements are exact saved Terra responses. A harness assertion initially blocked one slot before API use; it was resumed only after confirming zero requests for it. Missing Arabic close-up/product-feature aliases were corrected and replayed offline; no extra generation or softened claim gate was used. No final angle was omitted after that offline correction.

Exact stored quotes, review/theme IDs, unique counts, evidence unions and deterministic scores passed. Retained outputs passed manual checks for unsupported claims, product-target crossover, invented creator testimony, timeframe/frequency promises, localization and meaningful creative variety. These are evaluated dataset results, not a guarantee that lexical guards catch every future semantic error.

Final validation: 221 deterministic tests, typecheck, production build, five browser workflows and fresh real Supabase auth/session/relational persistence/RLS validation passed. Public email delivery/confirmation remains the separately user-confirmed prior pass. Secret cache cleanup remains effective; the final secret/customer-data audit is clean. Private credentials, customer datasets and validation artifacts remain ignored. Phase 3 has not started.
